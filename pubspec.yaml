name: zombie_2d
description: "UNDEAD SIEGE - A 2D top-down zombie survival game inspired by COD Zombies"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.21.0
  flame_audio: ^2.10.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
